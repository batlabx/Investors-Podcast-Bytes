# Investor Bytes — Run Report (2026-04-27)

## Status: ⚠️ DRAFT SAVED, NOT PUBLISHED — session can't reach credentials or state log

The episode was picked, classified, and a publication-ready draft was written and saved locally. **Publishing was not attempted** because this session has only `Investment Bytes` mounted. The two paths the task depends on are outside the connected folders:

- `/Users/batlab/Desktop/Batlab_Claude/.lenny-summarizer/credentials.json` — not accessible (Substack cookie lives here)
- `/Users/batlab/Desktop/Batlab_Claude/Scheduled_Tasks/.investor_bytes_published.json` — not accessible (state log)

Per the hard rules ("If anything fails, save the draft JSON and a clear error report — don't leave partial state"), I stopped before any HTTP call to Substack. The draft is sitting locally and can be published from a session that has `Batlab_Claude` mounted alongside `Investment Bytes`.

## What was picked

- **Filename:** `07_how-economics-and-art-shape-better-investors-w-kyle-grieve.txt`
- **Episode number:** 7
- **Original title:** *How Economics and Art Shape Better Investors*
- **Original date:** 28 March 2026
- **Original URL:** https://www.theinvestorspodcast.com/episodes/how-economics-and-art-shape-better-investors-w-kyle-grieve/
- **Random seed used:** `20260427` (date-seeded, deterministic for same-day reruns)
- **Pool size at pick time:** 115 transcripts matched `^[0-9]+_.*\.txt$`. Note: I could not subtract already-published filenames because the state log was unreachable. **Verify this episode hasn't already been published before posting.**

## Classification

- **Archetype:** Investing principles / frameworks
- **Sections used (5):** TL;DR · The Core Idea · Examples From The Episode · Where It Breaks Down · How To Use It
- **Word count:** 1,113 (target 800–1,400 ✓)

## Draft headline & subtitle

- **Title:** Why The Best Investors Think Like Movie Directors, Not Spreadsheet Jockeys
- **Subtitle:** Kyle Grieve on why scarcity, optimization, and narrative quietly run the market — even when you think you're just reading a 10-K.

## Files saved

Two files written into the only mounted folder (`Investment Bytes/_drafts_pending/`):

- `07_how-economics-and-art-shape-better-investors-w-kyle-grieve.html` — rendered HTML body (preview)
- `07_how-economics-and-art-shape-better-investors-w-kyle-grieve.json` — full publish payload: `{ metadata, draft_payload }` where `draft_payload` is the exact body to POST to `/api/v1/drafts` (ProseMirror JSON inside `draft_body` as a string, `draft_bylines: []`, `audience: "everyone"`, `type: "newsletter"`).

## To publish from this draft

In a session that has both `Batlab_Claude` and `Investment Bytes` mounted:

```python
import json, requests
creds = json.load(open("/Users/batlab/Desktop/Batlab_Claude/.lenny-summarizer/credentials.json"))
sid = creds["substack_session"]
BASE = "https://batlab.substack.com"
H = {"Cookie": f"substack.sid={sid}", "Content-Type": "application/json"}

draft = json.load(open("/Users/batlab/Desktop/Batlab_AI/Investment Bytes/_drafts_pending/07_how-economics-and-art-shape-better-investors-w-kyle-grieve.json"))["draft_payload"]
r = requests.post(f"{BASE}/api/v1/drafts", headers=H, json=draft, timeout=30); r.raise_for_status()
post_id = r.json()["id"]
p = requests.post(f"{BASE}/api/v1/drafts/{post_id}/publish", headers=H, json={"send": True}, timeout=30); p.raise_for_status()
print(p.json().get("url") or f"{BASE}/p/{p.json().get('slug')}")
```

Then append to `.investor_bytes_published.json`:

```json
{
  "filename": "07_how-economics-and-art-shape-better-investors-w-kyle-grieve.txt",
  "title": "Why The Best Investors Think Like Movie Directors, Not Spreadsheet Jockeys",
  "post_id": <fill in>,
  "url": "<fill in>",
  "archetype": "Investing principles / frameworks",
  "sections_used": ["TL;DR", "The Core Idea", "Examples From The Episode", "Where It Breaks Down", "How To Use It"],
  "published_at": "<ISO timestamp>"
}
```

## Warnings / caveats

- **Source transcript was thin.** This episode's `.txt` includes only the first ~3 minutes of actual transcript before being cut off — only the cold open and intro paragraphs survived the scrape. The "IN THIS EPISODE, YOU'LL LEARN" bullets and the books/resources block were used to scaffold the post. Two verbatim quotes from the intro are included; no quotes were invented. No specific numbers, prices, or stock data were fabricated. If the next sync of `tip-podcast-weekly-sync` produces fuller transcripts, the scraper at `Investment Bytes/scrape_tip.py` may want a look — most files in the folder appear to follow this same shortened pattern.
- **Couldn't deduplicate against state log.** Until the published log is checked, treat this draft as "candidate" rather than "guaranteed-fresh."
- **No write actions taken on Substack.** Hard rule respected: did not POST anything because publishing without the deduplication check + the credentials file would risk both republishing and exposing the wrong account.

## Fix the environment for next run

To make the scheduled task work end-to-end, the run environment needs these mounts:

- `~/Desktop/Batlab_AI/Investment Bytes` ✓ (already mounted)
- `~/Desktop/Batlab_Claude` ✗ (needs to be added — contains credentials, state log, and run-report directory)

Once both are mounted, the task runs autonomously without intervention.
